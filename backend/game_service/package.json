{
  "name": "game_service",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "dotenv -e .env.test -- npx prisma db push --schema=./database/prisma/schema.prisma && npx prisma generate --schema=./database/prisma/schema.prisma && jest",
    "dev": "dotenv -e .env -- npx prisma db push --schema=./database/prisma/schema.prisma && npx prisma generate --schema=./database/prisma/schema.prisma && nodemon"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "@fastify/cookie": "^11.0.2",
    "@fastify/env": "^5.0.2",
    "@prisma/client": "^6.12.0",
    "argon2": "^0.41.1",
    "axios": "^1.8.4",
    "fastify": "^5.3.0",
    "jsonwebtoken": "^9.0.2",
    "uuid": "^11.1.0"
  },
  "devDependencies": {
    "@fastify/cors": "^11.0.1",
    "nodemon": "^3.1.10",
    "prisma": "^6.12.0"
  }
}
