{
	"name": "transcendence",
	"version": "1.0.0",
	"private": true,
	"scripts": {
		"dev": "concurrently \"npm run dev --prefix backend/user_service\" \"npm run dev --prefix frontend\""
	},
	"devDependencies": {
		"axios": "^1.8.4",
		"concurrently": "^8.2.2"
  },
  "dependencies": {
    "@eleekku/logger": "^1.0.0"
	}
}
